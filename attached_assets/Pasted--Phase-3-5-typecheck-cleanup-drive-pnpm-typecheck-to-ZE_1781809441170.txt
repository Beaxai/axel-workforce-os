```
Phase 3.5 typecheck cleanup — drive pnpm typecheck to ZERO errors repo-wide. Work only on awf-os-brendy-sprint-1. Do NOT merge/PR/push to main. This is a TYPE-CORRECTNESS task only — no features, no refactors, no behavior changes. Ask me before doing anything outside this scope.

Give me your PLAN first and wait for my approval before editing any files or running git. In the plan, include: the exact git commands you'll use for Step 0, your fix approach for each error class, and (if you can run pnpm typecheck during planning) the categorized error list with the TS2769 sites you suspect are real bugs. Do not start until I approve.

STEP 0 — clear the push path first.
The unpushed history is 05f1535 -> d465956 (baseline script) -> ef418e3 (GitHub Actions workflow, tip). The workflow commit can't push with the current token (no workflow scope) and would block every later push. Uncommit it but keep the file: run `git reset HEAD~1` so .github/workflows/typecheck.yml becomes an untracked file (do NOT delete it, do NOT push it). Keep the d465956 baseline-script commit. Confirm the workflow file is no longer in the commit history of this branch's unpushed range.

Hard rules for the fixes:
- NO masking: do not disable/weaken any tsconfig option (noImplicitReturns, strictNullChecks, etc.), and no // @ts-ignore, // @ts-expect-error, or `as any`. Real fixes only.
- Behavior must not change.

STEP 1 — reproduce & categorize. Run pnpm typecheck and print the full list (file:line, code, message). Report before-counts per error code.

STEP 2 — fix TS2769 first (~17, Drizzle "no overload matches"). These are likely real bugs: insert/update payloads that don't match the current schema (stale/wrong columns, e.g. webhooks.ts). Fix the payload to match the schema. If any fix would change what data gets written, STOP and flag it to me with file/line — do not guess. Commit as its own commit. Report any genuine bugs uncovered.

STEP 3 — fix TS7030 (~96, "not all code paths return a value", Express handlers). Make each handler's returns CONSISTENT without altering control flow. Do NOT blindly prefix res.send/res.json with `return` where code is meant to run after it — review each handler. If a clean per-handler fix isn't possible, leave it and list it for me. Commit as its own commit.

STEP 4 — fix the single TS2345 at the source.

STEP 5 — lock it in. Update scripts/typecheck-baseline.sh so the api-server baseline is 0 (it currently allows 114).

STEP 6 — verify. Re-run pnpm typecheck and confirm ZERO errors across web, api-server, and libs. Confirm the API server boots and returns 200 on /api/healthz, and that POST /api/auth/login + GET /api/auth/me still work.

Then push the fix commits + the updated baseline script (the path is clear now). Report: before/after counts by code, the commit SHAs, any TS2769 fixes that revealed real bugs, and anything left unfixed with the reason.
```

Paste it in **Plan mode**. When its plan comes back, drop it here and I'll review before you approve.