# #40 Phase 4B — FINISH & VERIFY (acceptance run)

The build is merged. A review pass on branch `awf-os-brendy-sprint-1` pushed commit
`ba5d6bf` (fixes the active-deals stage filter `CLOSED_LOST` → `LOST`; removes two
stray work-order `.txt` dumps). Your job now is to land that commit and run the
Phase 4B acceptance tests to pass/fail — NOT to rebuild anything.

Branch: awf-os-brendy-sprint-1 only. Never touch main.

## 0. Sync first (audit, don't assume)
- `git fetch && git pull` on `awf-os-brendy-sprint-1` so you have `ba5d6bf`. Confirm
  HEAD == `ba5d6bf` (or later) before doing anything else; if you have local
  uncommitted Phase 4B work, reconcile it against this commit first.
- Confirm the canonical lost-stage literal in code is `"LOST"` (not `CLOSED_LOST`)
  in artifacts/api-server/src/lib/user-profiles.ts.

## 1. Migrate + seed (required before tests)
- Apply the `user_profiles` schema: `drizzle-kit push` — if it hangs on the known
  `deals` drift, apply the new table via direct SQL, keeping the Drizzle files as
  source of truth. Confirm `user_profiles` exists with: user_id (unique, FK cascade),
  title, timezone, bio, internal_notes, role_metadata jsonb, date_joined,
  last_login_at, updated_at.
- Run the status backfill once and paste the before/after status counts:
  the `normalize:user-status` script (per its header:
  `pnpm --filter @workspace/api-server run normalize:user-status`). Confirm every
  row is now in {active, invited, deactivated} and the 8 seeded users are `active`.
- Re-run the user seed (seed-users.ts) so each seeded user has a `user_profiles`
  row with role_metadata. Confirm the exact seed script name from
  artifacts/api-server/package.json before running.

## 2. Correctness gate
- `pnpm typecheck` — must be zero NEW errors vs the 3.5 baseline (state the baseline
  count; do not claim absolute zero if 3.5 left pre-existing errors).

## 3. Acceptance tests — run each, report PASS/FAIL with evidence
1. All 8 seeded users render complete, role-correct profiles at /users/:id
   (AGENT: agency + license + compliance + book summary; UNDERWRITER:
   lines/states/verticals from role_metadata; CSA/ADMIN: department/territory;
   CARRIER/PEO/VENDOR: company/programs; EMPLOYER: linked account).
2. Mini popover (UserMiniProfile) works from deal-team avatars, activity feed,
   task assignee, and global search — single shared component; no old AVATAR
   tooltip remains; non-avatar tooltips untouched.
3. AGENT book summary (deal count + premium, bound subset) matches a direct DB
   query (producing_agent_id = the agent; estimated_premium; bind_status = 'bound').
   Paste the API number and the SQL number side by side.
4. EMPLOYER GET /api/users/:id/profile of an unrelated user → 403; CARRIER/PEO see
   only internal staff + shared-deal profiles. Verify server-side (curl/Postman with
   each role's session), not just UI hiding.
5. Self-edit: phone/mobile/timezone PATCH /api/users/:id/profile persists
   (cross-table); self-edit of role/org/status returns 403 server-side, not hidden.
6. ADMIN POST /api/users/invite creates users + org_members + user_profiles
   atomically (verify all three rows; verify rollback leaves none on a forced
   failure). Approved agent registration (POST /api/agent-registrations/:id/approve)
   produces user + org_members + user_profiles with agency/license carried over,
   atomically, idempotent on email.
7. Status/login: a `deactivated` user is blocked at login (403 from the new status
   gate); an `invited` user becomes `active` only after completing reset; reactivate
   restores access. History (deals/activity) intact throughout.
8. last_login_at updates on a real login (existing handler) and surfaces in
   /admin/users (name, role, org, status, last login).
9. Design system in BOTH light AND dark on /users/:id, /profile, /admin/users, and
   the popover (tokens, glass recipes, pink primary / purple support). Attach
   light+dark screenshots of each.

## 4. Report
- One line per test: PASS/FAIL + evidence (response code, DB value, or screenshot).
- The phase is NOT done until all 9 pass. For any FAIL, post the failing output and
  stop for a fix rather than working around it.
- Flag for Curtis: a hard-delete endpoint `DELETE /api/users/:id` (ADMIN-only) still
  exists alongside the new deactivate flow — confirm whether to retire it
  (contradicts "no hard delete / preserve history") or keep it.
- On all-pass, update the State Document §9 status to done and note last_login +
  status-gate as auth-path changes that shipped.