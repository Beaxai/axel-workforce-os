PHASE D1.1 — ACCENT HIERARCHY FLIP (PINK PRIMARY / PURPLE SUPPORTING)
Revision to Phase D1: we are inverting the accent hierarchy. Pink is now the primary interactive color; purple becomes the supporting accent. The token architecture from D1 stays — only values and assignments change.
1. Token updates in src/index.css and use-theme-colors.ts:
--accent-primary:        #E91E8C   (was #7C3AED)
--accent-primary-hover:  #d1187e
--accent-primary-focus:  #ff4ba6   (dark-mode focus outline)
--accent-primary-soft:   rgba(233,30,140,0.15)
--accent-support:        #7C3AED   (purple — supporting accent)
--accent-support-hover:  #6D28D9
--accent-support-soft:   rgba(124,58,237,0.15)
--gradient-cta:          linear-gradient(135deg, #7C3AED 0%, #E91E8C 100%)   (unchanged)
Remove the now-redundant --accent-brand tokens — pink IS the primary; one token name per color.
2. Application changes:

All standard buttons (PrimaryButton), links, "View All" actions, GhostButton borders/text, focus rings, active nav indicator, selected tab underlines, loading states → pink --accent-primary
Gradient CTA rule unchanged: the single most-important action per screen keeps --gradient-cta; max one per view
Icon chips → --accent-support-soft background with purple icon (purple works well here — large soft fill, not small text)
Charts: pink = primary data series / donut emphasis; purple = secondary series and remaining harmonious palette
AxelBadge: restore pink variant to pink; add a purple variant; semantic status colors (green/red/yellow/blue/gray) untouched
Tailwind/shadcn HSL vars: --primary / --ring / --sidebar-primary → pink #E91E8C (HSL: 328 84% 52%) both modes; --chart-1 → pink, --chart-2 → purple
Avatar ring and brand marks remain pink (now consistent with primary)

3. Sweep: grep for #7C3AED and rgba(124,58,237 outside token definition files — all component references must go through tokens. Confirm #E91E8C likewise appears only in token files.
4. Acceptance checks:

All buttons, links, and active states render pink; no solid purple buttons remain
Purple appears only in: icon chips, badge purple variant, chart secondary series, gradient CTA
Focus rings pink (with --accent-primary-focus in dark mode)
Light/dark toggle correct on all affected components
Zero template blue anywhere (re-verify after HSL changes)
Screenshot Admin dashboard, Marketplace, and Pipeline after the change

No layout, spacing, typography, or logic changes.