Here it is again, unchanged and final — the blue question doesn't alter anything (status blue was already preserved):

PHASE D1 — DESIGN SYSTEM CONSOLIDATION + BRAND RESTYLE (PURPLE PRIMARY / PINK BRAND)
We are restyling the platform to match the Axel brand site: purple becomes the primary interactive accent, pink is reserved for brand moments and primary CTAs. We are also consolidating the currently-fragmented styling into a single token system. Inspect src/index.css, src/lib/use-theme-colors.ts, and all components in src/components/ before starting.
1. New design tokens — single source of truth
Define these as CSS variables in src/index.css AND expose them through use-theme-colors.ts so both styling paths read identical values:
--accent-primary:        #7C3AED   (primary interactive accent — purple)
--accent-primary-hover:  #6D28D9
--accent-primary-soft:   rgba(124,58,237,0.15)   (icon chips, tints, badge bg)
--accent-brand:          #E91E8C   (brand pink — restricted use, see rules)
--accent-brand-hover:    #d1187e
--accent-brand-focus:    #ff4ba6   (dark-mode focus outline variant)
--gradient-cta:          linear-gradient(135deg, #7C3AED 0%, #E91E8C 100%)
Canvas, glass, text, and border tokens are unchanged: #060608 dark canvas, #f4f4f5 light, existing text/border rgba values from use-theme-colors.ts.
2. Color application rules (enforce exactly)
PINK (--accent-brand) is restricted to:

The single primary CTA per screen — these use --gradient-cta as background (the ONLY permitted gradient), white text, hover = filter: brightness(1.1). Examples: "Generate Insight", "Quote Now", "Send Proposal", "Bind"
Active nav indicator bar (3px, solid pink — no gradient)
Selected tab underlines (solid pink)
Avatar ring / brand marks / login screen accent
Maximum one gradient CTA visible per view

PURPLE (--accent-primary) is everything else interactive:

All secondary/standard buttons (rename PinkButton → PrimaryButton, restyle to solid purple, hover --accent-primary-hover)
GhostButton: purple text + purple 1px border, hover fills purple
Links, "View All" actions, icons, icon chips (--accent-primary-soft bg + purple icon)
Chart/donut fills, focus rings, progress indicators, loading states
Badge "pink" variant in AxelBadge COLOR_MAP → remap to purple; keep green/red/yellow/blue/gray status colors unchanged (these are semantic status colors, not brand colors)

GRADIENT RULE (amended): --gradient-cta is permitted ONLY on the primary CTA button. Gradients remain banned on cards, panels, badges, charts, text, borders, and all other UI elements.
3. Kill the blue Tailwind/shadcn template theme
In src/index.css, repoint the HSL variable system to the real palette:

--primary / --ring / --sidebar-primary → purple #7C3AED (HSL: 262 83% 58%), both dark and light modes
--background dark → #060608 equivalent (not slate #0F172A) so shadcn components sit on the same canvas
--chart-1 → purple; replace remaining chart colors with a palette harmonious with purple/pink
Result: every shadcn ui/ primitive (inputs, dialogs, tables, selects, checkboxes, focus rings) renders on-brand — zero template blue remaining. Semantic status blue in badges/alerts is NOT affected by this.

4. Component cleanup

Delete legacy components/GlassCard.tsx (dark-only duplicate); migrate all its imports to components/ui/GlassCard.tsx
Rebuild components/StatCard.tsx on the theme-aware light-glass recipe; icon chip → --accent-primary-soft + purple icon
DealCardModal: its existing purple #7C3AED is now on-palette — keep it, but normalize its glass treatment from blur(48px) saturate(140%) to the standard heavy-glass recipe (blur 40px)
Standardize exactly two glass recipes: glass-card (rgba(255,255,255,0.05) bg, blur 12px, border 1px rgba(255,255,255,0.08), radius 12px) and glass-panel (rgba(18,18,24,0.82) bg, blur 40px, border 1px rgba(255,255,255,0.12), radius 16px, existing modal shadow) — with their existing light-mode counterparts
Replace the transparent --shadow-* placeholder tokens with real elevation values derived from the modal shadow currently in use
AppetiteBadge: migrate to tokens

5. Sweep all hardcoded values
Search the entire frontend for: #E91E8C, #d1187e, #ff4ba6, rgba(233,30,140, #2563EB, #3B82F6. Replace every instance with the appropriate token. No raw accent hex literals may remain in any component file — tokens only.
6. Acceptance checks — all must pass

Zero template blue (#2563EB/#3B82F6) renders anywhere — check shadcn inputs, dialogs, tables, focus rings. Semantic status blue in AxelBadge remains.
Grep for #E91E8C in src/ returns matches only in token definition files
Each dashboard view shows at most one gradient CTA; all other buttons solid purple
Active nav indicator is solid pink; donut chart and icon chips are purple
Light/dark toggle works correctly on all restyled components, including the rebuilt StatCard
No gradients anywhere except primary CTA buttons
Login page restyled: purple interactive elements, gradient CTA on the sign-in button, pink brand accent
Screenshot the Admin dashboard, Pipeline, and Login before/after and include in your report

Do not change layout, spacing, typography, or any business logic — this phase is color/token/component consolidation only.