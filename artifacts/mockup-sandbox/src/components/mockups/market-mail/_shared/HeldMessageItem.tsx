// Visual JSX source: axel-workforce-os/src/components/deal-card/HeldMessageItem.tsx.
import { useState } from "react";
import { Lock, ShieldAlert } from "lucide-react";
import type { Message } from "./types";
import { c } from "./types";
export function HeldMessageItem({ message, onRelease }: { message: Message; onRelease: () => void }) {
  const [confirmed, setConfirmed] = useState(false); const time = message.receivedAt ? new Date(message.receivedAt).toLocaleString("en-US", { month: "short", day: "numeric", hour: "numeric", minute: "2-digit" }) : "";
  return <div style={{ background: c.cardBg, border: "1px solid var(--accent-support)", borderRadius: 10, padding: 12, display: "flex", flexDirection: "column", gap: 8 }}><div style={{ display: "flex", justifyContent: "space-between", gap: 8 }}><div><div style={{ fontSize: 13, fontWeight: 600, color: c.textPrimary }}>{message.from.name || message.from.email}</div><div style={{ fontSize: 12, fontWeight: 500, color: c.textSecondary }}>Subject: {message.subject}</div></div><span style={{ fontSize: 11, color: c.textMuted }}>{time}</span></div><div style={{ fontSize: 11, color: c.textMuted }}>From: {message.from.email}<br />To: {message.to.join(", ")}<div style={{ display: "flex", gap: 8, marginTop: 4 }}><span style={{ color: "var(--accent-support)", fontWeight: 600 }}>Channel: {message.channel}</span><span style={{ paddingLeft: 8, borderLeft: `1px solid ${c.borderColor}` }}>Deal: {message.dealName}</span><span style={{ paddingLeft: 8, borderLeft: `1px solid ${c.borderColor}` }}>Thread: {message.threadLabel}</span></div></div><div style={{ fontSize: 13, color: c.textPrimary, lineHeight: 1.5 }}>{message.bodyText}</div>{message.candidate && <div style={{ marginTop: 12, paddingTop: 12, borderTop: `1px dashed ${c.borderColor}`, display: "flex", flexDirection: "column", gap: 10 }}><div style={{ fontSize: 12, color: "var(--accent-support)", display: "flex", gap: 6 }}><ShieldAlert size={14} style={{ flexShrink: 0 }} /><span><strong>Verify Sender:</strong> This message was matched to {message.candidate.marketName} but could not be cryptographically authenticated. Please manually verify the sender's address ({message.from.email}) is legitimate before releasing to the thread.</span></div><label style={{ display: "flex", gap: 8, fontSize: 12, color: c.textPrimary, cursor: "pointer" }}><input type="checkbox" checked={confirmed} onChange={e => setConfirmed(e.target.checked)} style={{ accentColor: "var(--accent-primary)" }} />I have manually verified that this sender ({message.from.email}) is authorized.</label><button disabled={!confirmed} onClick={onRelease} style={{ alignSelf: "flex-start", background: confirmed ? "var(--gradient-cta)" : c.hoverBg, color: confirmed ? "#fff" : c.textMuted, border: "none", borderRadius: 6, padding: "6px 14px", fontSize: 12, fontWeight: 600, display: "flex", alignItems: "center", gap: 6 }}><Lock size={14} /> Release to {message.candidate.marketName}</button></div>}</div>;
}